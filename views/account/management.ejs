<% if (title) { %>
  <h1><%= title %></h1>
<% } else {
  res.redirect('/')
} %>
<%- messages() %>

<% if (errors) { %>
  <ul class="notice-registration">
 <% errors.array().forEach(error => { %>
   <li><%= error.msg %></li>
<%  }) %>
 </ul>
<% } %>


<div class="sucess-log">
    <p>Welcome New</p>
    <p>You're logged in.</p>
    <a href="#" class="signup-link">Edit Account Information</a>
</div>